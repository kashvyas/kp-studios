[project]
name = "apex-launch-copilot"
version = "0.1.0"
description = "Synthetic AI-native PM proof artifact: tool use, retrieval, MCP, evals, and release gates"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
mcp = ["mcp[cli]>=2,<3"]
test = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
